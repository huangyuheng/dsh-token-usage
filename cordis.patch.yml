# dsh bundle patch: inserts this plugin into a profile's layer stack.
- insert:
    - id: dsh-token-use
      name: 'dsh-token-use'
      config:
        endpoint: /dsh-token-use
        scanAtBoot: true
